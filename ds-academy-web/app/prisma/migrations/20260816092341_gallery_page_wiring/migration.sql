-- AlterTable
ALTER TABLE "site_settings" ADD COLUMN     "galleryAdmHeading" TEXT NOT NULL DEFAULT 'Better still — come and see it.',
ADD COLUMN     "galleryAdmNote" TEXT NOT NULL DEFAULT 'Photographs are taken on the good days. Walk in on an ordinary one, sit through a class, and see the place as it actually runs.';
